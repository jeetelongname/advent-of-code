
;; takes in a number, downloads the input for that day,
;; stores it in a file.
;; return a string.

(defn get-input
  "
takes in a number, downloads the input for that day,
  stores it in a file.
  return a string.
"
  [day]
  :nope)
